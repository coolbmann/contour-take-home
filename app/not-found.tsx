import { NotFoundView } from "@/components/not-found-view";

/* Next's built-in not-found boundary — used for genuinely unknown URLs. */

export default function NotFound() {
  return <NotFoundView />;
}
